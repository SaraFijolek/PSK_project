@page
@model WebApplication1.Pages.Index1Model
@{
    ViewData["Title"] = "Analiza statystyczna";
}

<div class="container mt-5">
    <h1 class="mb-4">Analiza statystyczna wektora losowego (X, Y)</h1>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <h2 class="h5">Teoria</h2>
            <p>
                Kowariancja mierzy, jak dwie zmienne losowe zmieniają się razem.
                Korelacja określa stopień ich liniowej zależności.
            </p>
            <p>
                Dwie zmienne są <strong>stochastycznie niezależne</strong>,
                jeśli spełniają warunek \(P(X,Y)=P(X)\,P(Y)\).
                Są <strong>liniowo zależne</strong>, gdy ich współczynnik korelacji
                wynosi dokładnie 1 lub –1.
            </p>

            <h2 class="h5 mt-4">Treść zadania</h2>
            <p>Dana jest macierz \(P\) reprezentująca rozkład łączny:</p>
            <div class="table-responsive">
                <table class="table table-bordered w-auto">
                    <thead class="table-light">
                        <tr>
                            <th></th>
                            <th>\(Y=3\)</th>
                            <th>\(Y=2\)</th>
                            <th>\(Y=1\)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <th scope="row">\(X=1\)</th>
                            <td>0.1</td>
                            <td>0.2</td>
                            <td>0.3</td>
                        </tr>
                        <tr>
                            <th scope="row">\(X=2\)</th>
                            <td>0.1</td>
                            <td>0.1</td>
                            <td>0.2</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <p>oraz zbiory wartości: <strong>\(X(\Omega)=\{1,2\}\)</strong>, <strong>\(Y(\Omega)=\{3,2,1\}\)</strong>.</p>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <h2 class="h5 mb-3">Wyniki obliczeń</h2>
            <ul class="list-group list-group-flush">
                <li class="list-group-item">
                    <strong>(a) Cov(X,Y):</strong>
                    <span class="float-end">@Model.CovarianceResult.ToString("F3")</span>
                </li>
                <li class="list-group-item">
                    <strong>(b) ρ(X,Y):</strong>
                    <span class="float-end">@Model.CorrelationResult.ToString("F3")</span>
                </li>
                <li class="list-group-item">
                    <strong>(c) Niezależne?</strong>
                    <span class="float-end">@(Model.IsIndependent ? "Tak" : "Nie")</span>
                </li>
                <li class="list-group-item">
                    <strong>(d) Liniowo zależne?</strong>
                    <span class="float-end">@(Model.IsLinearlyDependent ? "Tak" : "Nie")</span>
                </li>
            </ul>
        </div>
    </div>

